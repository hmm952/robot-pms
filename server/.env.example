# 服务端口（与前端代理一致时为 3001）
PORT=3001

# JWT 密钥（生产环境请使用长随机字符串）
JWT_SECRET=nijintianhaomawojintianhenhaoganjintoujianli

# SQLite 数据库文件路径（相对于 server 目录）
DATABASE_PATH=./data/robot_pms.db

# —— 科大讯飞 RAG API（预留，接入时在 integrations 中读取）——
# IFLYTEK_RAG_BASE_URL=https://your-endpoint.example.com/v1
# IFLYTEK_RAG_API_KEY=
# IFLYTEK_RAG_APP_ID=
# IFLYTEK_RAG_TIMEOUT_MS=30000

# —— 邮箱 SMTP（预留，发件时在 mailService 中读取）——
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=
# SMTP_PASS=
# SMTP_FROM=noreply@example.com
